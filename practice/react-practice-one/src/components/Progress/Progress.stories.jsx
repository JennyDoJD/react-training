import Progress from './Progress';

const meta = {
  title: 'COMPONENTS/Common/Progress',
  tags: ['autodocs'],
};

export default meta;

export const Default = () => <Progress />;
Default.args = {};
